const initialState =[

    {text: "Hello World!"}
];

function notes(state=initialState,action){

    switch (action.types){

        default:
            return state;

    }
}

export default notes;
